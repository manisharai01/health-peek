# PyTorch CPU-only wheel index (must be first)
--extra-index-url https://download.pytorch.org/whl/cpu

# FastAPI and web framework dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
pydantic[email]>=2.0.0

# Database dependencies
motor==3.3.2
pymongo==4.6.0

# Authentication and security
python-jose[cryptography]==3.3.0
python-dotenv==1.0.0
passlib[bcrypt]==1.7.4

# Text processing and emoji support
emoji==2.8.0
regex==2023.10.3

# Multilingual language detection (lightweight, no GPU required)
langdetect==1.0.9

# Lightweight sentiment (no GPU required) - CPU-only torch for free tier
transformers>=4.35.2
torch>=2.9.0
tokenizers>=0.15.0
numpy>=1.24.3

# LLM via Groq API (cloud-based, no local model needed)
groq>=0.4.0

# NOTE: openai-whisper and ffmpeg-python removed for free tier (too heavy)
# Voice analysis will be disabled on free tier
